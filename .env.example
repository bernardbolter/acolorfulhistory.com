# WordPress GraphQL (fallback when PAYLOAD_API_URL is unset)
NEXT_PUBLIC_GRAPHQL_URL=https://artism.org/bolter/graphql

# Protomaps API key (map tiles)
NEXT_PUBLIC_PROTOMAPS=

# Payload CMS archive API (bernardbolter.com) — primary data source when set
PAYLOAD_API_URL=
PAYLOAD_API_KEY=
